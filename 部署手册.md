# 校园小卖部电商平台 —— 部署手册

---

## 一、环境要求

| 软件 | 版本 | 说明 |
|------|------|------|
| JDK | 17+ | Java 运行环境 |
| Maven | 3.8+ | 项目构建工具 |
| MySQL | 8.x | 数据库，端口 3306 |
| Redis | 6.x+ | 缓存服务，端口 6379 |
| MinIO | 最新版 | 文件存储，端口 9000 |

---

## 二、外部服务安装与配置

### 2.1 MySQL

1. 官网下载安装 MySQL 8.x，记住 root 密码
2. 登录 MySQL 创建数据库：
```sql
CREATE DATABASE code2026 DEFAULT CHARACTER SET utf8mb4;
```
3. 首次启动项目时，MyBatis 会自动建表（实体类上已配置），无需手动导入 SQL

### 2.2 Redis

1. Windows 下载地址：https://github.com/tporadowski/redis/releases
2. 安装后默认启动在 `localhost:6379`，无需密码
3. 验证：`redis-cli ping` → 返回 `PONG` 即正常

### 2.3 MinIO（文件服务器）

1. 下载地址：https://min.io/download
2. 启动（Windows 示例）：
```bash
minio.exe server C:\minio-data --console-address :9001
```
3. 访问 `http://127.0.0.1:9001` 进入控制台，用启动日志中的账号密码登录
4. 创建 Access Key：`admin` / `admin123`
5. 创建 Bucket（存储桶），名称设为 `code2026`，访问策略选 `public`（公开可读）

---

## 三、项目构建

```bash
# 进入项目根目录
cd E:\biyesheji\code2026

# 安装依赖并打包（跳过测试）
mvn clean package -DskipTests

# 打包完成后，可执行 jar 在：
#   springboot-backend\target\springboot-0.0.1-SNAPSHOT.jar   → 管理后台（端口9090）
#   miniapp-backend\target\miniapp-backend-0.0.1-SNAPSHOT.jar → 小程序后端（端口9091）
```

---

## 四、启动服务

### 方式一：命令行启动（开发环境）

```bash
# 启动管理后台（端口 9090）
cd E:\biyesheji\code2026\springboot-backend
mvn spring-boot:run

# 另开一个终端，启动小程序后端（端口 9091）
cd E:\biyesheji\code2026\miniapp-backend
mvn spring-boot:run
```

### 方式二：java -jar 启动（打包后）

```bash
# 启动管理后台
java -jar springboot-backend\target\springboot-0.0.1-SNAPSHOT.jar

# 启动小程序后端
java -jar miniapp-backend\target\miniapp-backend-0.0.1-SNAPSHOT.jar
```

### 启动成功标志

看到以下日志即启动成功：
```
Tomcat started on port 9090
Tomcat started on port 9091
```

---

## 五、访问地址一览

### 5.1 后端服务

| 服务 | 地址 | 说明 |
|------|------|------|
| Web 管理后台 API | `http://localhost:9090` | 管理员登录、商品管理、订单管理等 |
| 健康检查 | `http://localhost:9090/actuator/health` | 返回 `{"status":"UP"}` |
| 微信小程序 API | `http://localhost:9091` | 小程序端所有接口 |
| 健康检查 | `http://localhost:9091/actuator/health` | 返回 `{"status":"UP"}` |

### 5.2 外部服务

| 服务 | 地址 | 说明 |
|------|------|------|
| MinIO 控制台 | `http://127.0.0.1:9001` | 管理文件（上传/下载/删除） |
| MinIO API | `http://127.0.0.1:9000` | 文件访问地址（程序中配置的 endpoint） |

### 5.3 前端（独立部署）

| 前端 | 技术栈 | 对应后端 |
|------|------|------|
| Web 管理后台 | Vue/React（独立项目） | `http://localhost:9090` |
| 微信小程序 | 微信开发者工具 | `http://localhost:9091` |

---

## 六、配置速查

配置文件位置：
- 管理后台：`springboot-backend\src\main\resources\application.yml`
- 小程序后端：`miniapp-backend\src\main\resources\application.yml`

**需要根据实际情况修改的配置项**：

| 配置项 | 默认值 | 说明 |
|------|------|------|
| MySQL 密码 | `123456` | 改成你本地 MySQL 的 root 密码 |
| Redis 密码 | 空 | 如果 Redis 设了密码，填这里 |
| MinIO 账号/密码 | `admin` / `admin123` | 改成你创建的 Access Key |
| 微信 AppID/AppSecret | 已填写 | 换成你自己的小程序信息 |
| DeepSeek API Key | 已填写 | 换成你的 DeepSeek API 密钥 |
| `fileBaseUrl` | `http://localhost` | 部署到服务器后改成服务器 IP 或域名 |

---

## 七、常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| 启动报 `Connection refused` | MySQL 或 Redis 没启动 | 确保 MySQL(3306)、Redis(6379) 已启动 |
| 图片上传失败 | MinIO 未启动或 Bucket 未创建 | 启动 MinIO，在控制台创建名为 `code2026` 的 Bucket |
| 端口被占用 | 9090 或 9091 已被其他程序使用 | 修改 `application.yml` 中 `server.port` 的值 |
| XML 解析报错 | mapper XML 文件格式问题 | 执行 `mvn clean compile` 重新编译 |
| 验证码不显示 | Redis 未连接 | 检查 Redis 是否启动，密码是否正确 |

---

## 八、启动顺序总结

```
1. 启动 MySQL   → 端口 3306
2. 启动 Redis   → 端口 6379
3. 启动 MinIO   → 端口 9000（API）/ 9001（控制台）
4. 启动 springboot-backend  → 端口 9090
5. 启动 miniapp-backend     → 端口 9091
6. 启动前端项目（独立仓库） → 分别指向对应端口
```
